#ifndef HELP_FUNCTIONS_H_INCLUDED
#define HELP_FUNCTIONS_H_INCLUDED

#include <stdio.h>
#include <math.h>

int bitAt(int, int);
int clearBit(int, int);
int setBit(int, int);
int reverseBit(int, int);
int setEvenToNull(int);
int countBits(int);

#endif //HELP_FUNCTIONS_H_INCLUDED